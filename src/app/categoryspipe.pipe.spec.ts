import { CategoryspipePipe } from './categoryspipe.pipe';

describe('CategoryspipePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryspipePipe();
    expect(pipe).toBeTruthy();
  });
});
