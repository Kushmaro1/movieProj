import { MoviedoublePipe } from './moviedouble.pipe';

describe('MoviedoublePipe', () => {
  it('create an instance', () => {
    const pipe = new MoviedoublePipe();
    expect(pipe).toBeTruthy();
  });
});
